#ifndef MUITILS_H
#define MUTILS_H

#include <pthread.h>

/* =============================================================================
 * mutils_lock
 * =============================================================================
 */
void mutils_lock(pthread_mutex_t* mutex);


/* =============================================================================
 * mutils_lock
 * =============================================================================
 */
void mutils_trylock(pthread_mutex_t* mutex);


/* =============================================================================
 * mutils_unlock
 * =============================================================================
 */
void mutils_unlock(pthread_mutex_t* mutex);

#endif
